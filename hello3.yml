class: GalaxyWorkflow

doc: Simple workflow that cats a File.

inputs:
  the_input:
    type: File

outputs:
  the_output:
    outputSource: cat/out_file1

steps:
  cat:
    tool_id: cat1
    in:
      input1: the_input
