class: GalaxyWorkflow

inputs:
  the_subject:
    type: string
    default: "World"

outputs:
  the_output:
    hello/the_output

steps:
  hello:
    run:
      class: GalaxyTool
      command: echo 'Hello' $the_subject > $the_output
      outputs:
        the_output:
          format: txt


